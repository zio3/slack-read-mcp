# 導入手順（AI 向け）

この文書は、**利用者に代わって導入作業を行う AI** に向けたものです。
人間向けの説明は [README.md](README.md) を参照してください。

## 手順0: どの版を読んでいるか名乗る

**作業を始める前に、この文書の版を利用者に報告してください。** AI の Web 取得は
キャッシュにより古い版を掴むことがあり、それが実際に起きています。自分では
「最新かどうか」を判定できない前提で行動してください。

- **リポジトリを clone して、その中の INSTALL.md を読むのが正**です。Web 上の表示より
  clone を信用してください
- clone 後に `git log -1 --format="%h %cd" --date=iso` を実行し、**コミットのハッシュと
  日時を利用者に報告**してから作業を始めてください
- 利用者が「それは古い」と言ったら、`git pull` して読み直してください

この文書に `~/.slack-read-mcp`（`.config` を挟まないパス）や「トークンをコピーした状態で
コマンドを実行するよう利用者に依頼する手順」が書かれていたら、**それは旧版です。**

利用者は「Slack を読めるようにして」程度の依頼しかしません。
環境を確認し、適切な方式を選び、**利用者にしかできない操作だけを依頼**してください。

## 大原則

**トークンを会話に出させない。** 利用者に「トークンを貼ってください」と言ってはいけません。
利用者にはトークンを**コピーだけ**してもらい、ファイルへの書き込みは AI がクリップボードから
直接行います（手順4に詳細。値を表示しなければ会話ログには残りません）。

**Slack 側の操作は利用者が行う。** アプリの作成・インストール・チャンネルへの招待は、
ブラウザでの操作と権限が要ります。AI が代行しようとしないでください。

## 手順1: 前提を確認する

```bash
node --version
```

Node.js が無ければ、利用者にインストールを依頼してください。v18 以上が必要です。

次に、**過去の設定が残っていないかを確認します。**

```
~/.config/slack-read-mcp/ が既に存在するか
```

存在する場合、それが**現役の設定なのか、過去のテストの残骸なのか**を利用者に確認して
ください。残骸を黙って流用すると、「新しく設定できた」ように見えて実際は古い token と
channels.json で動く事故になります。再テストや再インストールの場合は、先にディレクトリ
ごと削除してから進めてください。

次に、**Slack アプリの現在の状態を確認します**。作る人と使う人は別のことが多く、
また「既に作ってある」場合もあります。次の三択で尋ねてください。

> Slack アプリの状況はどれですか？
> A. 管理者からトークンを渡されている（アプリは自分で管理しない）
> B. 自分のアプリを作成済みで、トークンも取得できる
> C. まだ何もない（これから作る）

**A. トークンを渡されている（組織導入の利用者）** — 手順2は丸ごと飛ばして手順3へ。
このルートでは Slack 側の操作はすべて管理者の仕事です。

- アプリの作成・インストールは済んでいる前提。利用者に確認するのは
  「Token（`xoxb-` で始まる）を受け取ったか」だけ
- **チャンネルへの Bot 招待も管理者に依頼**してもらいます。利用者がゲストの場合、
  自分では招待できないことがあります。「読みたいチャンネルを管理者に伝えて、
  Bot の招待を依頼してください」と案内してください
- 読めないチャンネルがあった場合（`not_in_channel` 等）の解決先も管理者です

**B. 作成済みでトークンも手元にある** — 手順2は飛ばして手順3へ。
A と違い、チャンネルへの招待（`/invite`）や読めないときの対処は**利用者自身**が行えます。

**C. これから作る（個人利用・管理者自身）** — 手順2へ進みます。

## 手順2: Slack アプリを用意してもらう（ルート B のみ）

**この作業は利用者が行います。** 次を提示してください。

1. [api.slack.com/apps](https://api.slack.com/apps) を開く
2. **Create New App** → **From a manifest** を選ぶ
3. ワークスペースを選び、`slack-app-manifest.json` の内容を貼り付ける
4. **Create** → **Install to Workspace** → 許可する
5. **OAuth & Permissions** に表示される **Bot User OAuth Token**（`xoxb-…`）を控える
6. 読みたいチャンネルで `/invite @slack-read-mcp` を実行する

補足として伝えるとよいこと。

- 招待していないチャンネルは読めません。**インストールしただけでは一件も読めません**
- プライベートチャンネルも、招待すれば読めます。招待しなければ存在も見えません
- Slack フリープランはインストールできるアプリが10個までです。上限に達している場合、不要なアプリを1つ外す必要があります

## 手順3: 起動方式を決める

**既定は npx です。クローンもビルドも不要**で、取得時に自動でビルドされます。
Node のバージョンによる分岐もありません。迷う要素がなければこれを選んでください。

```
npx -y github:zio3/slack-read-mcp
```

**リポジトリをクローンするのは、次のいずれかに当てはまる場合だけ**です。

- ネットワーク経由の取得を避けたい
- 実行するコードをレビューしてから使いたい（動くコードと読むコードを一致させたい）
- このサーバー自体を改造・開発する

クローンした場合の起動方法は Node のバージョンで変わります。

| Node のバージョン | 起動コマンド |
|---|---|
| v18 〜 v22.5 | `npm install && npm run build` → `node dist/index.js` |
| v22.6 〜 v23.5 | `node --experimental-strip-types src/index.ts`（ビルド不要） |
| v23.6 以上 | `node src/index.ts`（ビルド不要） |

迷ったらビルド方式（1行目）を選んでください。どのバージョンでも動きます。
TypeScript の直接実行は experimental のため stderr に警告が出ますが、動作に支障は
ありません（MCP は stdout を使います）。

## 手順4: 設定ディレクトリを用意する

設定はホームディレクトリの `~/.config/slack-read-mcp/` に置きます。**環境変数は使いません**
（環境変数の変更はターミナルごと再起動しないと反映されず、導入時の典型的な詰まりに
なるため。ファイル方式ならクライアントの再起動だけで済みます）。

```
~/.config/slack-read-mcp/
├── token           Bot User OAuth Token（xoxb-…）を1行だけ
└── channels.json   読み取り対象のチャンネル一覧
```

### token（利用者がコピー → AI が配置する）

役割分担は「**利用者はコピーだけ、書き込みは AI**」です。

利用者に配置コマンドをコピペしてもらう方式は**構造的に破綻している**ので使わないでください。
トークンをコピーした手でコマンドをコピーした瞬間、クリップボードが上書きされてトークンが
消えます。また Claude Code の `!` 実行は Bash のため、PowerShell のコマンドを貼っても
構文エラーになります（実際に起きた失敗です）。

正しい流れ:

1. 利用者に「Slack アプリの OAuth & Permissions ページで Bot User OAuth Token の
   **Copy** を押してください」と依頼する。**これが利用者の最後のコピー操作**になるよう、
   以降コピーを要求しない
2. 「コピーしましたか？」を確認する（選択式の質問が出せるならそれを使う）
3. AI がクリップボードから**直接ファイルへ書き込む**。値は表示しない

Windows での書き込み（AI が実行する。出力にトークンを含めないこと）:

```powershell
$t = (Get-Clipboard -Raw).Trim()
if (-not $t) { "クリップボードが空です。コピーし直してください" }
elseif ($t -notlike "xoxb-*") { "xoxb- で始まっていません（長さ $($t.Length)）。コピーし直してください" }
else {
  New-Item -ItemType Directory -Force "$env:USERPROFILE\.config\slack-read-mcp" | Out-Null
  [System.IO.File]::WriteAllText("$env:USERPROFILE\.config\slack-read-mcp\token", $t)
  Set-Clipboard "slack-read-mcp: token placed"
  "配置しました（長さ $($t.Length)）。クリップボードは上書き済みです"
}
```

報告してよいのは**長さと形式チェックの結果だけ**です。値の一部でも表示しないでください。
最後の `Set-Clipboard` は、**本物のトークンをクリップボードに残さないため**の上書きです。

利用者がトークンを会話に貼ろうとしたら、止めてこの流れを案内してください。
すでに貼られてしまった場合は、**Slack アプリの Reinstall でトークンを再発行する**よう
伝えてください。

### channels.json（AI が作ってよい）

`channels.example.json` を参考に、対象チャンネルを列挙します。

```json
[
  { "id": "C0123456789", "name": "team-general" },
  { "id": "https://example.slack.com/archives/C0123ABCDEF" }
]
```

- `id` は **Slack の URL のままでもよい**（ID を自動抽出します）。利用者には「チャンネル名を
  右クリック →『リンクをコピー』して、その URL をそのまま教えてください」と依頼するのが
  最も簡単です
- `name` は表示用のラベルにすぎず、**省略できます**。読み取りは ID だけで行われるため、
  name が実際のチャンネル名と違っていても動きます（違和感の原因にはなるので、分かる範囲で
  正しく付けるのが望ましい）

なお、チャンネル一覧を Slack から自動取得して利用者に選ばせる導線は**作れません**
（一覧系スコープを意図的に付与していないため `missing_scope` になります）。ID は利用者から
聞くしかありません。これは仕様です。

**この設定ファイルはアクセス制御ではありません。** ここに何を書いても、Bot が招待されて
いないチャンネルは読めません。利用者が自由に編集して構わない旨を伝えてください。

## 手順5: MCP クライアントへ登録する

**推奨はローカルスコープ**（この Slack に関わる作業をするフォルダにだけ登録する）です。

この MCP は特定のワークスペースのデータへの窓です。ユーザースコープで入れると全プロジェクト
のセッションに常駐し、**無関係な作業の文脈からもその Slack が読める**状態になります。複数の
顧客・業務を1台の PC で扱う場合、これは情報の混線経路になります。ツールが見える範囲を業務の
フォルダに一致させてください。ユーザースコープが許容できるのは、その PC を単一業務専用に
している場合だけです。

**プロジェクトスコープは不適切です。** リポジトリ内に設定ファイル（`.mcp.json`）を作るため、
共有時に他人へ自分用の登録を配ってしまいます。

Claude Code の場合:

```bash
claude mcp add slack-read --scope local -- npx -y github:zio3/slack-read-mcp
```

ローカルのクローンを使う場合:

```bash
claude mcp add slack-read --scope local -- node /path/to/slack-read-mcp/dist/index.js
```

Codex CLI の場合は `~/.codex/config.toml` に追記します（ユーザー単位の設定であり、
リポジトリには入らないためスコープの問題はありません）:

```toml
[mcp_servers.slack-read]
command = "npx"
args = ["-y", "github:zio3/slack-read-mcp"]
```

`npx` の初回実行は取得とビルドで時間がかかり、クライアントの起動タイムアウトに
かかることがあります。事前に一度 `npx -y github:zio3/slack-read-mcp --help` などで
温めておくか、クローンして `command = "node"`, `args = ["/path/to/dist/index.js"]` を
指定してください。

温め実行は、トークン未配置の段階では**「Bot Token が見つかりません」で exit 1 になるのが
正常**です。取得とビルドはその時点で完了しており、失敗ではありません。

**トークンを MCP の設定に書かないでください**（`--env SLACK_BOT_TOKEN=…` を使わない）。
サーバーは `~/.config/slack-read-mcp/token` を自分で読みます。設定に平文で残すと、設定の
共有・バックアップ時に漏れます。

登録後、MCP クライアントを再起動（または再接続）してください。

## 手順6: 動作を確認する

再起動後、次の順で確認します。

1. `list_channels` … 設定ファイルの内容が返るか
2. `get_channel_history` … 招待済みチャンネルのメッセージが読めるか

エラーが出た場合、原因は次のいずれかです。

| エラー | 原因 | 対処 |
|---|---|---|
| `not_in_channel` | パブリックチャンネルに Bot が招待されていない | Bot の招待が必要。自分で作った場合は `/invite`、組織導入なら管理者に依頼 |
| `channel_not_found` | プライベートチャンネルで未招待、または ID が誤り | 同上。ID も確認 |
| `missing_scope` | 権限が付与されていない操作を呼んだ | 仕様。回避しようとしない |
| `invalid_auth` / `account_inactive` | トークンが無効、またはアプリがアンインストール済み | 再インストールとトークン再取得を依頼 |
| `Connection closed` / サーバーが起動しない | トークンが見つからない | `~/.config/slack-read-mcp/token` が存在するか、中身が1行の `xoxb-…` かを確認 |

**読めなかった理由を推測で埋めないでください。** エラーをそのまま伝えれば、利用者は
招待や再設定で解決できます。

## 導入後

利用者の AI（あなた自身、または後で使われる AI）に、**[GUIDE.md](GUIDE.md) を読ませてください。**

Slack のデータ構造には、知らないと取りこぼしに気づけない箇所があります。特に
`get_channel_history` はスレッド返信を返しません。これを知らずに使うと、返信のやり取りを
丸ごと見落とし、しかも見落としたことに気づけません。
